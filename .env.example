# =============================================================================
# Media Concierge Bot - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git!
# =============================================================================

# -----------------------------------------------------------------------------
# TELEGRAM
# -----------------------------------------------------------------------------
# Get from @BotFather on Telegram
# 1. Message @BotFather
# 2. Send /newbot
# 3. Follow instructions
# 4. Copy the token
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# -----------------------------------------------------------------------------
# ANTHROPIC (Claude AI)
# -----------------------------------------------------------------------------
# Get from https://console.anthropic.com/settings/keys
# 1. Sign in to console.anthropic.com
# 2. Go to API Keys
# 3. Create new key
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# TMDB (The Movie Database)
# -----------------------------------------------------------------------------
# Get from https://www.themoviedb.org/settings/api
# 1. Create account on themoviedb.org
# 2. Go to Settings > API
# 3. Request API key (choose "Developer")
TMDB_API_KEY=your_tmdb_api_key_here

# -----------------------------------------------------------------------------
# KINOPOISK (unofficial API)
# -----------------------------------------------------------------------------
# Get from https://kinopoiskapiunofficial.tech/
# 1. Register on the site
# 2. Get your API token
KINOPOISK_API_TOKEN=your_kinopoisk_token

# -----------------------------------------------------------------------------
# SEEDBOX (optional - leave empty if not using)
# -----------------------------------------------------------------------------
# Your seedbox connection details
# Supports Transmission, qBittorrent, Deluge
# If not configured, bot will show magnet links instead of auto-downloading
SEEDBOX_HOST=
SEEDBOX_USER=
SEEDBOX_PASSWORD=
SEEDBOX_TYPE=transmission  # transmission, qbittorrent, or deluge

# -----------------------------------------------------------------------------
# SECURITY
# -----------------------------------------------------------------------------
# Encryption key for storing OAuth tokens
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-32-byte-key-here-base64-encoded

# -----------------------------------------------------------------------------
# OPTIONAL SETTINGS
# -----------------------------------------------------------------------------
# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Cache TTL in seconds (default 1 hour)
CACHE_TTL=3600

# Database path
DATABASE_PATH=data/bot.db

# Webhook settings for Koyeb deployment
# Leave empty for local development (uses polling)
WEBHOOK_URL=
WEBHOOK_PATH=/webhook
PORT=8000

# Health check server port (used by Docker/Koyeb health checks)
HEALTH_PORT=8080

# Environment (development or production)
# In production mode with WEBHOOK_URL set, bot uses webhook mode
# Bot username for deep links (without @)
BOT_USERNAME=yourbotname

ENVIRONMENT=development
